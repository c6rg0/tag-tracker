[project]
name = "tagtracker"
version = "0.1.0"
description = "Tracks new github releases (tags)"
readme = "README.md"
requires-python = ">=3.15"
dependencies = [
    "requests>=2.34.2",
    "tabulate>=0.10.0",
]
